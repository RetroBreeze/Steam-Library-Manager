from __future__ import annotations

from rich.console import Console


def run_tui() -> None:
    Console().print(
        "[yellow]Full Textual TUI is not implemented yet.[/yellow]\n"
        "Use `slm search`, `slm list`, `slm install`, and `slm uninstall` for the MVP."
    )

